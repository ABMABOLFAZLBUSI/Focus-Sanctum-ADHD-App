name: Build Android APK

on:
  push:
    branches: [ "main", "master" ]
  pull_request:
    branches: [ "main", "master" ]

jobs:
  build:
    name: Build Focus Sanctum Web & Android APK
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Source Code
        uses: actions/checkout@v4

      - name: Set up Java (JDK 24)
        uses: actions/setup-java@v4
        with:
          distribution: 'zulu'
          java-version: '24'

      - name: Set up Node.js (24.18.0)
        uses: actions/setup-node@v4
        with:
          node-version: '24.18.0'
          cache: 'npm'

      - name: Install Node Dependencies
        run: npm ci

      - name: Build Production Web Assets
        run: npm run build

      # =========================================================================
      # OPTIONAL: Native Android APK Packaging Step
      # If you are packaging using Capacitor, you can uncomment these steps
      # inside your repository to automatically compile the final Android APK:
      # =========================================================================
      #
      # - name: Sync Assets to Android Project
      #   run: |
      #     npm install @capacitor/core @capacitor/cli @capacitor/android
      #     npx cap sync android
      #
      # - name: Setup Android SDK
      #   uses: android-actions/setup-android@v3
      #
      # - name: Compile Debug APK
      #   run: |
      #     cd android
      #     ./gradlew assembleDebug
      #
      # - name: Upload Built APK Artifact
      #   uses: actions/upload-artifact@v4
      #   with:
      #     name: focus-sanctum-app-debug
      #     path: android/app/build/outputs/apk/debug/app-debug.apk
